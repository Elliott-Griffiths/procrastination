---
// Daily Procrastination Mantras component
const mantras = [
    "I'll do it later.",
    "Future me will handle it.",
    "Is this *really* due today?",
    "A watched pot never boils... or gets that report written."
];
---

<section class="bg-yellow-50 p-6 rounded-xl shadow-inner border border-yellow-200">
    <h2 class="text-2xl md:text-3xl font-bold text-yellow-800 mb-4">
        Daily Procrastination Mantras 🙏
    </h2>
    <div class="flex flex-wrap gap-4 justify-center">
        {mantras.map((mantra) => (
            <span class="bg-yellow-200 text-yellow-800 px-4 py-2 rounded-full text-sm font-medium shadow-sm">
                "{mantra}"
            </span>
        ))}
    </div>
</section>